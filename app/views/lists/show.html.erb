<p>
  <strong>Title:</strong>
  <%= @list.title %>
</p>
 
<p>
  <strong>Description:</strong>
  <%= @list.description %>
</p>

<td><%= link_to 'Edit', edit_list_path(@list) %></td>
<%= link_to 'Back', lists_path %>